1й метод: создаем метод для разделения строки из консоли в массив по знаку.
2й метод: создаем метод сортирующий символы из массива. отсчитываем количество,  создаем массив по выясненому количеству.
3й метод: создаем метод для вывода информации из массивов.

информационная строка для пользователя в консоле: "Введите набор символов через знак (,) - "

присваиваем введенное в консоле от пользователя символы string inStringSimvol = console.ReadLine()
присваиваем массиву преобразованную строку через метод string[] arraySimvol = GetArrayStringConsole(inStringSimvol)

Console.Write("Введенные символы - "); 
Print(GetArrayStringConsole(inStringSimvol)); 
Console.Write("Введенные символы длинной меньше либо равны 3 - ");
Print(GetArrayThreeSimvol(arraySimvol)); Console.WriteLine();